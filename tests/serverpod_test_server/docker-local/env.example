# Copy to .env in this directory if host ports 8090 / 8091 are already taken.
# Then set database.port and redis.port in ../config/development.yaml to match.
SERVERPOD_TEST_POSTGRES_PORT=8090
SERVERPOD_TEST_REDIS_PORT=8091

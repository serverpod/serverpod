apiServer:
  port: 8080
  publicHost: localhost
  publicPort: 8080
  publicScheme: http

insightsServer:
  port: 8081
  publicHost: localhost
  publicPort: 8081
  publicScheme: http

database:
  host: localhost
  port: 8090
  name: serverpod_test
  user: postgres

redis:
  enabled: true
  host: localhost
  port: 8091

class: DatabaseMigrationWarning
fields:
  type: DatabaseMigrationWarningType
  message: String
  table: String
  columns: List<String>
  # jsonKey keeps backward compatibility with migration.json files that use the misspelled key.
  destructive: bool, jsonKey=destrucive
name: serverpod_relic
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  mustache_template: ^2.0.0
  serverpod:
    path: ../serverpod

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

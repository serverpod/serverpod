/// Database constants used by the serverpod framework.
abstract class DatabaseConstants {
  /// Current version of the database definition.
  static const databaseDefinitionVersion = 1;

  /// Current version of the database migration.
  static const databaseMigrationVersion = 1;
}

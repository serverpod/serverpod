### A pair of refresh and access tokens, in their external format.
class: TokenPair
fields:
  ### The refresh token to be used to create new access tokens and roate itself.
  ###
  ### The client should handle this as an opaque string.
  ## This is stored as `$refreshTokenId:$refreshToken`
  refreshToken: String

  ### The latest access token, encoded as a JWT.
  accessToken: String

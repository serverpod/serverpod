class: ActiveSession
table: serverpod_auth_session
fields:
  # TODO: Switch to UUID once possible
  # id: UuidValue

  # TODO: Change to UUID type
  ### The id of the [AuthUser] this session belongs to
  userId: int, relation(parent=auth_user)

  ### The time when this sesion was created.
  created: DateTime

  sessionKey: String

### Defines a reason for a failed sign in.
enum: AuthenticationFailReason
serialized: byIndex
values:
  - invalidCredentials
  - userCreationDenied
  - internalError
  - tooManyFailedAttempts
  - blocked

/// The brand color preset of the Microsoft Sign-In button.
///
/// Applies only when the button stands on its own; inside a [SignInWidget] the
/// shared common style takes over.
enum MicrosoftButtonStyle {
  /// Light background with dark text (Microsoft's default).
  light,

  /// Dark background with white text.
  dark,
}

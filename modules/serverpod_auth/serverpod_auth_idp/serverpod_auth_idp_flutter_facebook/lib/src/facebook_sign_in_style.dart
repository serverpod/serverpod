/// The brand color preset of the Facebook Sign-In button.
///
/// Applies only when the button stands on its own; inside a [SignInWidget] the
/// shared common style takes over.
enum FacebookButtonStyle {
  /// Facebook blue button (default).
  blue,

  /// White button with Facebook blue text.
  white,
}

# TEMPLATE

publish_to: none

name: serverpod_test_shared_module_server
version: 1.0.0
description: Serverpod module fixture that owns a shared table package.
repository: https://github.com/serverpod/serverpod

environment:
  sdk: DART_VERSION

resolution: workspace

dependencies:
  serverpod: SERVERPOD_VERSION
  serverpod_test_shared_module_shared:
    path: ../serverpod_test_shared_module_shared

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: SERVERPOD_VERSION
  test: ^1.25.5

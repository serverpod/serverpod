# TEMPLATE

publish_to: none

name: serverpod_test_sqlite_flutter
version: 1.0.0
description: Flutter web tests for Serverpod client-side databases.
repository: https://github.com/serverpod/serverpod

environment:
  sdk: DART_VERSION
  flutter: FLUTTER_VERSION

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  serverpod_test_sqlite_client:
    path: ../serverpod_test_sqlite_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  serverpod_database: SERVERPOD_VERSION

flutter:
  uses-material-design: true

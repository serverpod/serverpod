# TEMPLATE

name: serverpod_push_store_client
description: Client for the Serverpod push notification store.
version: SERVERPOD_VERSION

environment:
  sdk: DART_VERSION

resolution: workspace

dependencies:
  serverpod_client: SERVERPOD_VERSION
  serverpod_push_core_client: SERVERPOD_VERSION

dev_dependencies:
  serverpod_lints: SERVERPOD_VERSION
  test: ^1.25.5

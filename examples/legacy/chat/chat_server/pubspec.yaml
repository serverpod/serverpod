# This file is generated. Do not modify, instead edit the files in the templates/pubspecs directory.
# Mode: development

# Frozen at Serverpod 3.5.0-beta.12 together with the legacy chat module, see
# issue #5443. The chat packages are unpublished, hence the path overrides.

name: chat_server
description: Starting point for a Serverpod server.

environment:
  sdk: '^3.10.3'

dependencies:
  serverpod: 3.5.0-beta.12
  serverpod_auth_server: 3.5.0-beta.12
  serverpod_chat_server: 3.5.0-beta.12

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'

dependency_overrides:
  serverpod_chat_server:
    path: ../../../../modules/legacy/serverpod_chat/serverpod_chat_server
